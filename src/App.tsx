/**
 * @format
 */

import React from 'react';
import {SafeAreaView, StatusBar} from 'react-native';

const App = () => {
  return (
    <SafeAreaView>
      <StatusBar />
    </SafeAreaView>
  );
};

export default App;
